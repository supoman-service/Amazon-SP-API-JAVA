/*
 * Selling Partner API for Fulfillment Inbound
 * The Selling Partner API for Fulfillment Inbound lets you create applications that create and update inbound shipments of inventory to Amazon's fulfillment network.
 *
 * OpenAPI spec version: v0
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */


package com.amazon.spapi.model.fulfillmentinbound;

import java.util.Objects;
import java.util.Arrays;

import com.amazon.spapi.client.ApiException;
import io.swagger.annotations.ApiModel;
import com.google.gson.annotations.SerializedName;

import java.io.IOException;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;

/**
 * @author: ty
 * @description: 货件状态枚举类
 * @date: 2021/8/9
 */
@JsonAdapter(TransportStatus.Adapter.class)
public enum TransportStatus {

  /**
   * 处理中：
   * 您已成功调用此货件的 putTransportDetails 操作，但尚未调用estimateTransport 操作
   *
   */
  WORKING("WORKING"),

  /**
   * 正在估算货件费用中：
   * 您已成功调用此货件的estimateTransport 操作，承运人正在估算货件成本。
   */
  ESTIMATING("ESTIMATING"),

  /**
   * 估算货件费用完成：
   * 承运人已完成估算装运成本的过程。您的运输请求已准备好由您确认。
   */
  ESTIMATED("ESTIMATED"),

  /**
   * 估算费用异常：
   * 调用estimateTransport 操作时出现问题，并返回了错误。
   */
  ERROR_ON_ESTIMATING("ERROR_ON_ESTIMATING"),

  /**
   * 正在确认物流信息中：
   * 您已成功调用confirmTransport 操作，但确认过程尚未完成。
   */
  CONFIRMING("CONFIRMING"),

  /**
   * 物流信息确认完成：
   * 您的物流运输请求已被确认。
   * 重要提示：对于小包裹货件，您最多可以在确认后 24 小时内取消运输请求。对于非整车/整车 (LTL/FTL) 货件，您可以在确认后一小时内取消运输请求。宽限期过后，卖家账户将被收取运费
   */
  CONFIRMED("CONFIRMED"),

  /**
   * 物流信息确认异常：
   * 您调用 confirmTransport 操作时出现问题，并返回了错误。
   */
  ERROR_ON_CONFIRMING("ERROR_ON_CONFIRMING"),

  /**
   * 取消物流运输中：
   * 您已成功为已确认的承运人货件调用 voidTransport 操作，但该操作尚未完成。
   */
  VOIDING("VOIDING"),

  /**
   * 取消物流运输完成：
   * 您确认的承运人货件已作废。卖家账户不会收取运费。
   */
  VOIDED("VOIDED"),

  /**
   * 调用取消物流异常：
   * 您调用 voidTransport 操作时出现问题，并返回了错误。
   */
  ERROR_IN_VOIDING("ERROR_IN_VOIDING"),

  /**
   * 请求异常：
   */
  ERROR("ERROR");

  private String value;

  TransportStatus(String value) {
    this.value = value;
  }

  public String getValue() {
    return value;
  }

  @Override
  public String toString() {
    return String.valueOf(value);
  }

  public static TransportStatus fromValue(String text) {
    for (TransportStatus b : TransportStatus.values()) {
      if (String.valueOf(b.value).equals(text)) {
        return b;
      }
    }
    return null;
  }

  public static class Adapter extends TypeAdapter<TransportStatus> {
    @Override
    public void write(final JsonWriter jsonWriter, final TransportStatus enumeration) throws IOException {
      jsonWriter.value(enumeration.getValue());
    }

    @Override
    public TransportStatus read(final JsonReader jsonReader) throws IOException {
      String value = jsonReader.nextString();
      return TransportStatus.fromValue(String.valueOf(value));
    }
  }
}

